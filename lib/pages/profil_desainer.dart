import 'package:flutter/material.dart';

class ProfilDesainer extends StatefulWidget {
  const ProfilDesainer({Key? key}) : super(key: key);

  @override
  State<ProfilDesainer> createState() => _ProfilDesainerState();
}

class _ProfilDesainerState extends State<ProfilDesainer> {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
    );
  }
}
